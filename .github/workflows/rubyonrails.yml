name: Ruby on Rails CI
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
jobs:
  test:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.head_ref }}-test
      cancel-in-progress: true
    timeout-minutes: 2
    services:
      postgres:
        image: postgres:16.1
        ports:
          - "5432:5432"
        env:
          POSTGRES_DB: hostedgpt_test
          POSTGRES_USER: rails
          POSTGRES_PASSWORD: password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      RAILS_ENV: test
      DATABASE_URL: "postgres://rails:password@localhost:5432/hostedgpt_test"
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Set up database schema
        run: bin/rails db:prepare

      - name: Build CSS
        run: bin/rails tailwindcss:build

      - name: Run tests
        run: bin/rails test

  system_tests:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.head_ref }}-system
      cancel-in-progress: true
    services:
      postgres:
        image: postgres:16.1
        ports:
          - "5432:5432"
        env:
          POSTGRES_DB: hostedgpt_test
          POSTGRES_USER: rails
          POSTGRES_PASSWORD: password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      RAILS_ENV: test
      DATABASE_URL: "postgres://rails:password@localhost:5432/hostedgpt_test"
      DISPLAY: "=:99"

    steps:
      # - name: Print Chrome Version
      #   run: google-chrome --version
      # - name: Update BrowserList DB
      #   run: npx update-browserslist-db@latest

      # - name: Setup Chrome
      #   run: npx @puppeteer/browsers install chrome@stable

      - name: Install needed software
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb x11-apps x11-xkb-utils libx11-6 libx11-xcb1 libnss3-dev libgdk-pixbuf2.0-dev libgtk-3-dev libxss-dev gconf-service libasound2 libatk1.0-0 libc6 libcairo2 libcups2 libdbus-1-3 libexpat1 libfontconfig1 libgcc1 libgconf-2-4 libgdk-pixbuf2.0-0 libglib2.0-0 libgtk-3-0 libnspr4 libpango-1.0-0 libpangocairo-1.0-0 libstdc++6 libx11-6 libx11-xcb1 libxcb1 libxcomposite1 libxcursor1 libxdamage1 libxext6 libxfixes3 libxi6 libxrandr2 libxrender1 libxss1 libxtst6 ca-certificates fonts-liberation libnss3 lsb-release xdg-utils wget ca-certificates

      # - name: Setup ChromeDriver
      #   run: npx @puppeteer/browsers install chromedriver@stable
      # run: npx chromedriver --detect

      # - name: Install Tmate
      #   run: |
      #     sudo apt-get update
      #     sudo apt-get install tmate -y

      # - name: Create tmate config file
      #   run: |
      #     echo "set tmate-api-key \"tmk-ueeA20OSLbXNCtHh6k2nYShS2V\"" > ~/.tmate.conf
      #     echo "set tmate-session-name \"jlvallelonga\"" >> ~/.tmate.conf
      #     cat ~/.tmate.conf

      # - name: Setup tmate session
      #   run: |
      #     tmate -S /tmp/tmate.sock new-session -d
      #     tmate -S /tmp/tmate.sock wait tmate-ready
      #     tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}'
      #     sleep 20

      - name: Set DEBUG env var to true
        run: export DEBUG=true

      # - name: Print env vars
      #   run: printenv
      # run: printenv | grep DEBUG

      # - name: Start SSH session
      #   uses: luchihoratiu/debug-via-ssh@main
      #   with:
      #     NGROK_AUTH_TOKEN: "7WrCqSNJW3aPVjEgQ2fm1_7WCcnpW3rmUNxLH1jasis"
      #     SSH_PASS: password1234
      #     NGROK_REGION: ap

      # - name: Where is ChromeDriver
      #   run: which chromedriver

      # uses: browser-actions/setup-chrome@v1
      # with:
      #   install-chromedriver: true
      #   install-dependencies: true

      # - name: Print ChromeDriver Version
      #   run: chromedriver --version

      # - name: Print Chrome Version Again
      #   run: google-chrome --version
      # - name: LS ChromeDriver
      #   run: ls /root/.cache/selenium/chromedriver/linux64

      # - name: Print Chrome Version Again
      #   run: google-chrome --version

      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Set up database schema
        run: bin/rails db:prepare

      - name: Build CSS
        run: bin/rails tailwindcss:build

      - name: which user
        run: whoami

      - name: Run system tests
        run: |
          sed -i 's/hoverOnlyWhenSupported: true/hoverOnlyWhenSupported: false/' config/tailwind.config.js
          bin/rails test:system

      # - name: Sleep for 60 seconds
      #   run: sleep 60

      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: System Test Screenshots
          path: tmp/screenshots/
          if-no-files-found: ignore

  lint:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.head_ref }}-lint
      cancel-in-progress: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Lint Ruby files
        run: bin/rubocop
